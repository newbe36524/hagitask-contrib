## 运行模式
当前提示运行在非交互环境中。

### 非交互规则
- 不要使用 AskUserQuestion 或任何交互式确认工具。
- 当需要可选决策时，直接选择合理默认值，并记录该假设。
- 跳过可选确认，直接继续执行。

## 任务
根据下面解析后的作用域，更新所选项目的代理文档：
- `AGENTS.md` 是主要文档数据源。
- `CLAUDE.md` 保持极简重定向，仅指向 `AGENTS.md`。

## 执行上下文
- 工作目录：`{{projectPath}}`
{{#if repositoryPath}}- 解析得到的仓库提示：`{{repositoryPath}}`
{{/if}}
- 解析得到的作用域键：`{{scope}}`

### 目标文件范围

{{{targetScopeMarkdown}}}

## 要求
1. 修改前先阅读现有的 `AGENTS.md` 与 `CLAUDE.md`。
2. 变更必须严格限制在目标文件范围中明确列出的文件。
3. 将 `AGENTS.md` 视为主要文档数据源；所有实质性指令、命令、约定和代理行为约束都放在 `AGENTS.md` 中。
4. 保持 `CLAUDE.md` 极简，只用于重定向，参考模式：
   ```
   # Claude 配置
   此文件指向 [AGENTS.md](./AGENTS.md)，后者是项目特定 AI 助手配置的唯一事实来源。
   ```
5. 当 `scope` 为 `project-only` 或 `root-only` 时，更新根目录 `AGENTS.md`（主要内容）与 `CLAUDE.md`（重定向）。
6. 当 `scope` 为 `root-and-children` 时，更新范围块中列出的所有 `AGENTS.md` / `CLAUDE.md` 文件，并对每个目标都遵循 AGENTS 为主模式。
7. 保留仍然准确的现有说明；仅在被更好的当前说明替换时移除过时内容。
8. 优先复用相邻仓库已有文档约定，不要发明新的维护格式。
9. 若列出的文件缺失，或某个子仓库还没有现成维护文档，必须明确记录假设。
10. 默认使用用户输入所用语言回复；若混合语言，以最后一个明确语言指令为准。
11. 若作用域内没有可写目标文件，只进行只读分析，并给出可执行的后续修改建议。

## 输出格式
- 必须按以下顺序输出：
  1. `## 结论`
  2. `## 假设（如有）`
  3. `## 下一步`
- 当无假设时，在 `## 假设（如有）` 下写 `无`。
- 若做出假设，必须使用固定格式：`假设：<一句话>；原因：<一句话>；影响：<一句话>`。
- 结果必须包含：修改文件列表与采用的作用域。

## 建议执行流程
1. 检查所选项目根目录，以及范围中列出的子仓库文件。
2. 读取每个目标的现有 `AGENTS.md` 与 `CLAUDE.md` 内容。
3. 对照仓库当前真实工具链与工作流，识别过时文档。
4. 先更新 `AGENTS.md`，作为完整事实来源。
5. 再更新 `CLAUDE.md`，保持极简重定向。
6. 总结结果与未解决缺口。
