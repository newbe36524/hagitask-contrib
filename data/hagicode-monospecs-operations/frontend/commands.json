{
  "$schema": "https://tasks.hagicode.com/schemas/task-preset-plugin/commands.schema.json",
  "schemaVersion": "1.1",
  "selectionMode": "single",
  "groups": [
    {
      "id": "monospecs",
      "title": { "key": "commands.groups.monospecs" },
      "description": { "key": "commands.groups.monospecsDescription" }
    }
  ],
  "commands": [
    {
      "id": "initialize",
      "groupId": "monospecs",
      "label": "initialize",
      "description": { "key": "commands.items.initialize.description" },
      "preludeTemplate": "Use the initialize operation template. The operation description is optional for this command."
    },
    {
      "id": "add-repository",
      "groupId": "monospecs",
      "label": "add-repository",
      "description": { "key": "commands.items.addRepository.description" },
      "preludeTemplate": "Use the add-repository operation template. Apply the supplied operation description to identify the repository URL or URLs."
    },
    {
      "id": "reorder-repositories",
      "groupId": "monospecs",
      "label": "reorder-repositories",
      "description": { "key": "commands.items.reorderRepositories.description" },
      "preludeTemplate": "Use the reorder-repositories operation template. The operation description is optional for this command."
    }
  ]
}
