## 任务

根据以下规范化仓库范围更新项目代理文档。

- 操作：`{{command}}`
- 包含 CLAUDE.md：`{{includeClaudeMd}}`
- 包含类别：{{#if agentsInclude}}{{agentsInclude}}{{else}}未指定{{/if}}
- 排除类别：{{#if agentsExclude}}{{agentsExclude}}{{else}}未指定{{/if}}
- 操作目标与验收标准：

{{operationBrief}}

### 规范化仓库范围

{{{targetScopeMarkdown}}}

### 执行要求

1. 修改前先读取每个目标中的 AGENTS.md。
2. 所有变更严格限制在规范化仓库列表内，不得自行扩大路径。
3. 所有实质性说明写入 AGENTS.md。
4. includeClaudeMd 为 false 时，不得读取、创建或修改 CLAUDE.md。
5. includeClaudeMd 为 true 时，只创建或更新指向 AGENTS.md 的极简 CLAUDE.md。
6. 按要求报告修改文件、作用域和假设。
